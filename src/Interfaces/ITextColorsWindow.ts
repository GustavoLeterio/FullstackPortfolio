import { WindowNames } from '../Types/TWindowNames';

export interface TextColorsWindow {
  windowName: String;
  title: String;
  primary: String;
  secondary: String;
  light: String;
  dark: String;
}
